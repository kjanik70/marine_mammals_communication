# Audio: Small model on toothed cetaceans only (sperm whales, orcas, dolphins)
model:
  preset: small
  vocab_size: 1026
  max_seq_len: 512
  dropout: 0.15

data:
  dataset_type: audio
  token_dir: data/tokenized/toothed
  max_seq_len: 512
  augment: true

training:
  learning_rate: 3.0e-4
  weight_decay: 0.1
  batch_size: 32
  num_epochs: 200
  warmup_steps: 300
  min_lr_ratio: 0.01
  log_interval: 10
  eval_interval: 100
  save_interval: 500
  patience: 30
  output_dir: runs/audio_small_toothed
